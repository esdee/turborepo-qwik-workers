name: Deploy Worker

on:
  push:
    branches:
      - main

env:
  CLOUDFLARE_API_TOKEN: ${{secrets.CF_PAGES_TOKEN}}
  CLOUDFLARE_ACCOUNT_ID: ${{secrets.CF_ACCOUNT_ID}}

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Publish API Pages
    steps:
      - name: Check out code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - uses: pnpm/action-setup@v2.2.4
        with:
          version: latest

      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: pnpm install

      - name: Test
        run: pnpm run test

      - name: Publish Web Pages
        run: export CLOUDFLARE_API_TOKEN=${{ secrets.CF_PAGES_TOKEN }} && cd ./apps/web && pnpm run publish
